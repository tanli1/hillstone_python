习目的
熟悉python的字符串数据类型的相关操作，包括索引，查找，打印，分割，比较，替换，删除，截取，切片，复制等

练习内容
1. 生成一个字符串，"abcdefghijkl123mnopqrstuvwxyz<> 123def4567890 [] #&*"
2. 计算字符个数
3. 往字符头部添加字符"+"，往字符尾部添加字符"-"
4. 将字符d替换成\d，将小写字母替换成大写字母
5. 去掉空格
6. 去掉字母和数字
7. 将每个字符都用"="号隔开
8. 将字符按1/2/3/4/5......个字符,用"="隔开，就"a=bc=def=hijk=......"
9. 字符串反转
10. 字符串复制n=2次

相关知识点
字符串是python的内置数据类型，且是一种不可变序列，具备序列类型都支持的操作：
（以下的s、t皆表示序列，x表示元素）
x in s  # 若s包含x，返回True，否则返回False
x not in s  # 若s包含x，返回False，否则返回True
s + t  # 连接两个序列
s * n  # s复制n次
s[i]   # s的索引第i项
s[i:j] # s切片从第i项到第j-1项
s[i:j:k]  #  s切片从第i项到第j-1项,间隔为k
len(s)  # s的长度
min(s)  # s的最小元素
max(s)  # s的最大元素
s.index（x） # x的索引位置
s.count(x)  # s中出现x的总次数